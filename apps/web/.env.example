# Keep API/RPC same-origin (`/api`, `/rpc`).
# Local development is handled by Vite proxy in vite.config.ts.

# Public base URL for profile images served from R2/public CDN.
VITE_PROFILE_IMAGES_PUBLIC_BASE_URL=
